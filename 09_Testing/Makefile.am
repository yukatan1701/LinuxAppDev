SUBDIRS =src
GENERATES = *~ aclocal.m4 autom4te.cache compile config.guess config.* configure depcomp install-sh libtool ltmain.sh Makefile Makefile.in missing stamp-h1 .deps .libs

clean:
	rm -rf $(GENERATES)

